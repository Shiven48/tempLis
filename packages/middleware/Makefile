.PHONY: run-graphics run-middleware run-all test clean

run-graphics:
	python ./src/graphics/window.py

test:
	@echo "No tests configured for middleware yet."

clean:
	find ./src -type d -name "__pycache__" -exec rm -rf {} +
	rm -rf .pytest_cache