.PHONY: run test clean

run:
	uvicorn api.main:app --port 8000 --reload --app-dir ./src

test:
	# Add tests for the api here
	@echo "No tests configured for api yet."

clean:
	@rm -rf __pycache__ .pytest_cache
